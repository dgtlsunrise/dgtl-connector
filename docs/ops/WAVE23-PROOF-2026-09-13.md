# Wave 23 proof — support / audit / docs / marketplace honesty (FINAL)

Date: 2026-09-13  
Branch: `cursor/wave23-final-support-docs-e355`  
Base: `2bcbbd8` (Wave 22 recs approve push + 566-pass proof)  
Goal: Production operability for Waves 10–22. Close doctor / `support_packet` / runbook gaps, keep marketplace / public copy **Consent A-only**, keep `catalog.json` / `validate-spec` honest (kernel **26**, no write promises), touch agency isolation. No new named mutate tools.

## What shipped

| Item | Detail |
| --- | --- |
| Consent G runbook | Stale “Wave 10 plumbing / no Admin mutate” copy removed. `CONSENT_G_REQUIRED` now points at `auth login-ga4-admin` + `DGTL_WRITES_ENABLED` + confirm IDs |
| Runbooks | `META_NOT_CONNECTED`, `TIKTOK_SCOPE_MISSING`; WRITE / Klaviyo scope copy includes productSet, catalog, send-job, catalogs/reviews |
| `support_packet` | `license.sgtm` + `conversion_fabric` (`polar_sgtm` reserved/off/not-minted + `apply_key_present` boolean). Never keys / JWT / hop URLs |
| Doctor | `DGTL_SGTM_APPLY_KEY` / `DGTL_APPLY_KEY` listed as SET/UNSET names only |
| Marketplace honesty | `plugin.json` / `package.json` stay read-only; [MARKETPLACE.md](MARKETPLACE.md) + [TOOLS.md](../TOOLS.md) separate listing copy from operator write consents |
| Agency isolation | Shopify shop, Klaviyo account, `merchant_id`, `act_`, `advertiser_id` — no cross-client catalog joins |
| Spec lock | `validate-spec.py` `check_marketplace_honesty`; kernel **26**; `local_free=36`; `skills=21` |

Named closed tools only. Consent A kernel stays **26**. Never Axos. Do **not** publish site, Worker, or marketplace (Noel RED). Do not invent Polar checkout URLs. Do not change Consent A Data Access / write scopes.

## Locks held

- Consent A / free Desktop client stays readonly (`analytics.readonly` + `webmasters.readonly` + `tagmanager.readonly` on the listing client).
- Write consents (W / G / S / MC / Shopify / Klaviyo) stay out of marketplace title/description.
- Polar `sgtm` reserved, default-off, **not minted**.
- `catalog.json` `count` / `tools[]` stay the 26-tool kernel. Writes remain `gated_tools`.
- No mega-mutate. No new MCP tools. `tools/list` freeze stays **164**.
- Never Axos.

## Spec honesty

`python3 scripts/validate-spec.py` expected:

```
SPEC OK  tools=26 (Consent A kernel)  local_free=36  skills=21
```

Hop maps `--check` clean (no hop-catalog edit). `tools/list` freeze count **164** (unchanged).

## Files (primary)

- `src/support/runbooks.ts`, `src/support/packet.ts`, `src/auth/doctor.ts`, `src/errors.ts`
- `docs/ops/RUNBOOKS.md`, `docs/ERRORS.md`, `docs/TOOLS.md`, `docs/MARKETPLACE.md`, `docs/PERMISSIONS.md`, `docs/SKILLS.md`, `docs/SUPPORT_AND_CLIENTS.md`
- `README.md`, `SECURITY.md`, `package.json`
- `skills/agency-property-isolation/SKILL.md`, `skills/google-marketing-support/SKILL.md`, `skills/license-and-reconnect/SKILL.md`
- `scripts/validate-spec.py`
- `tests/wave23-ops.test.ts`, `tests/support-packet.test.ts`, `tests/wave9-allowlists.test.ts`, `tests/fixtures/w0-3-support-packet.json`
- Docs: this file + [MEGA-WAVE-2-COMPLETE-ROLLUP-2026-09-13.md](MEGA-WAVE-2-COMPLETE-ROLLUP-2026-09-13.md)

## Tests

Run: `npm test` (fixture / unit only; network guard; no live stamp / Google / Meta / TikTok / Klaviyo). `python3 scripts/validate-spec.py`. `npm run generate:hop:check`.

**Local result (2026-09-13):** recorded after CI-local run on this branch (Wave 22 was `566`; Wave 23 adds `tests/wave23-ops.test.ts`). Expected `SPEC OK  tools=26 (Consent A kernel)  local_free=36  skills=21`.

| Suite | Role |
| --- | --- |
| `tests/wave23-ops.test.ts` | Kernel 26 / no writes in listing; Consent G runbook; packet fabric + G/S/Klaviyo; doctor apply-key names; marketplace + agency copy; validate-spec counts |
| `tests/support-packet.test.ts` / `tests/doctor.test.ts` / `tests/wave9-allowlists.test.ts` | Packet keys; license.sgtm; runbooks for G/S/W + Meta/TikTok scope |

## Noel atoms (console — leftover; live hangover gates remain external)

1. Do **not** add write / Ads / GBP scopes to the free Consent A Data Access screen.
2. Do **not** publish site, Worker, or marketplace from this PR.
3. Stamp hop-catalog is unchanged — no stamp parity PR required for Wave 23.
4. Orchestrator FF-merge if Origin cannot merge: use this branch tip after the proof commit.

## Rollup

Waves **10–23** are complete in this repo (code + docs). See [MEGA-WAVE-2-COMPLETE-ROLLUP-2026-09-13.md](MEGA-WAVE-2-COMPLETE-ROLLUP-2026-09-13.md).
