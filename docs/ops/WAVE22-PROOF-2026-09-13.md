# Wave 22 proof — recs → approve → push

Date: 2026-09-13  
Branch: `cursor/wave22-recs-approve-push-80a0`  
Base: `c2c9508` (Wave 21 MTA/LTV budget)  
Goal: One skill sequences Ads recommendations, MC issues, GTM workspace vs live, Klaviyo flow status, and a missing GA4 Ads link. **One mutate tool per confirm.** Optional batch `gads_apply_recommendations` with an explicit RN list in confirm. Optional late `klaviyo_create_campaign_send_job` with a `SEND` token — confirm-gated and **cannot** fire from draft create.

## What shipped

| Item | Detail |
| --- | --- |
| Skill `recs-approve-push` | Read five surfaces, then one named mutate per confirm |
| `gads_apply_recommendations` | Stamp `POST /{ADS_API_VERSION}/customers/{customer_id}/recommendations:apply`. Explicit `recommendation_resource_names[]`. Confirm must include `customer_id` **and each RN**. Refuse `apply_all` / `*` / empty list |
| `gads_apply_recommendation` | Still one RN. Same locks: no apply-all; **ENABLED is not a side effect** |
| `klaviyo_create_campaign_send_job` | Local `pk_`. `POST /api/campaign-send-jobs`. `dry_run` default true. Live confirm = account id + campaign id + **`SEND`** |
| `klaviyo_create_campaign` | Remains draft-only. `send_job` / `send` refuse. Never hits send-jobs |
| Fixtures | `fixtures/ads/recommendations.three.json` (one confirmed → one mutate HTTP); `fixtures/gtm/workspace-diff.json`; `fixtures/klaviyo/campaign-send-job.create.json` |

Named closed tools + skill only. Consent A kernel stays **26**. Never Axos. No Wave 23 marketplace rewrite.

## Stamp hop (parity contract)

Copy `src/gateway/hop-catalog.json` **byte-identical** into stamp (including the new `tools[]` row), then `node scripts/generate-hop-maps.mjs`.

Hop row (plugin + stamp must match):

```json
{
  "name": "gads_apply_recommendations",
  "family": "gads",
  "kind": "mutate",
  "method": "POST",
  "path_template": "/{ADS_API_VERSION}/customers/{customer_id}/recommendations:apply"
}
```

Stamp builder must apply **only** the named RNs. Do **not** implement apply-all. Do **not** set campaign/ad `status=ENABLED` as a side effect of apply.

Klaviyo send is **not** a stamp hop (local `pk_`).

## Locks held

- No auto-apply all Google recs.
- ENABLED must not be a side effect of apply. Status flips stay on `gads_set_*` with explicit `status` + confirm.
- One mutate tool per confirm.
- Draft create cannot fire a send job.
- Consent A untouched (kernel **26**). Never Axos.
- No Wave 23 marketplace rewrite beyond this skill/docs.

## Spec honesty

`python3 scripts/validate-spec.py` expected:

```
SPEC OK  tools=26 (Consent A kernel)  local_free=36  skills=21
```

Hop maps `--check` clean. `tools/list` freeze count **164** (additive).

## Files (primary)

- `src/recs/approve-push.ts`
- `src/ads/gads-wave2.ts` (`gads_apply_recommendations` + apply locks)
- `src/klaviyo/klaviyo-write.ts` (`klaviyo_create_campaign_send_job`)
- `src/klaviyo/http.ts` (allowlisted `POST /api/campaign-send-jobs`)
- `src/gateway/hop-catalog.json` + generated hop maps / `w0-2-mutate-parity.json`
- `src/tools/registry.ts`, `src/tools/schemas.ts`
- `schemas/v1/catalog.json`, `tools.schema.json`
- `fixtures/ads/recommendations.three.json`, `fixtures/gtm/workspace-diff.json`, `fixtures/klaviyo/campaign-send-job.create.json`
- `skills/recs-approve-push/SKILL.md`
- `tests/wave22-recs.test.ts`
- Docs: `docs/TOOLS.md`, `docs/SKILLS.md`, `docs/PERMISSIONS.md`

## Tests

Run: `npm test` (fixture / unit only; network guard; no live stamp / Ads / Klaviyo send).

**Local result (2026-09-13):** `566` tests, `0` fail (Wave 21 was `557`; Wave 22 adds `9` in `tests/wave22-recs.test.ts`). `python3 scripts/validate-spec.py` → `SPEC OK  tools=26 (Consent A kernel)  local_free=36  skills=21`. Hop maps `--check` clean.

| Suite | Role |
| --- | --- |
| `tests/wave22-recs.test.ts` | Three-recs fixture: one confirmed → one mutate HTTP; apply-all / ENABLED refuse zero hop; draft create never hits send-jobs; SEND token gate; five-surface sequence; skill laws |
| `tests/klaviyo-readonly.test.ts` / `tests/klaviyo-write.test.ts` | LOCAL_FREE send tool; draft still never send-jobs |
| `tests/wave9-allowlists.test.ts` / `allowlist-parity.test.ts` | Hop maps; mutate parity; tools/list freeze 164 |

## Noel atoms (console — leftover)

1. Stamp parity PR: copy hop-catalog (new `gads_apply_recommendations` row + comment) byte-identical; Worker builder applies only named RNs; never apply-all; never set ENABLED.
2. Live apply / send only on disposable DGTL/test accounts — fixtures preferred.
3. Orchestrator FF-merge if Origin cannot merge: use this branch tip after the proof commit.
