# Wave 21 proof — MTA / LTV → budget

Date: 2026-09-13  
Branch: `cursor/wave21-mta-ltv-budget-34c9`  
Base: `44a27f4` (Wave 20 conversion fabric)  
Goal: Chat can compare GA4 DDA (Ads-id dimensions) to Ads last-click recipes, propose budgets, and call existing confirm-gated budget tools **one platform at a time**. No mega `allocate_budgets`.

## What shipped

| Item | Detail |
| --- | --- |
| `gads_search` recipes | Closed `click_view` (ClickView, single-day — plugin refuses a multi-day range before hop), `keyword_performance` (`keyword_view`), `ad_performance` (`ad_group_ad`). Plugin hops **recipe name only**. No raw GAQL. |
| Fixtures | `fixtures/ads/recipes.{click_view,keyword_performance,ad_performance}.json` — stamp-parity SELECT-only GAQL |
| Skill `mta-ltv-budget` | Consent A GA4 attribution + metadata + Ads-id reports; Ads last-click is a second series; LTV only if metadata lists it; dry_run; one-platform confirm |
| `tiktok_update_campaign_budget` | Named mutate. Stamp `POST /open_api/{TIKTOK_API_VERSION}/campaign/update/`. dry_run + confirm `advertiser_id` AND `campaign_id`. Dual-gate `TIKTOK_MUTATE_ENABLED`. Spend cap $100,000 |
| Refusals | Missing metadata apiNames; invented `userLifetimeValue`; `gclid` on `ga4_run_report`; two-platform confirm |

Named closed tools only. Consent A kernel stays **26**. Never Axos. No Wave 22 recs polish.

## Stamp hop / recipe compilers (parity contract)

Copy `src/gateway/hop-catalog.json` **byte-identical** into stamp (including the new `tools[]` row), then `node scripts/generate-hop-maps.mjs`.

Hop row (plugin + stamp must match):

```json
{
  "name": "tiktok_update_campaign_budget",
  "family": "tiktok",
  "kind": "mutate",
  "method": "POST",
  "path_template": "/open_api/{TIKTOK_API_VERSION}/campaign/update/"
}
```

Stamp `gads_search` must compile these recipe names (plugin never sends GAQL):

| recipe | FROM | date_range | note |
| --- | --- | --- | --- |
| `click_view` | `click_view` | single day | `click_view.gclid` is Ads-only |
| `keyword_performance` | `keyword_view` | required | criterion id; not GA4 keyword text |
| `ad_performance` | `ad_group_ad` | required | join GA4 on creative id, not gclid |

Closed SELECT lists live in `src/ads/recipes-gaql.ts` + the fixtures above.

## Locks held

- Consent A for GA4 reports / metadata / attribution GET. Consent C + Pro for Ads. Meta / TikTok Pro.
- Never Axos. Never mega `allocate_budgets`.
- Consent A kernel unchanged (**26**).
- Ads last-click is not sole truth.
- Do not invent `userLifetimeValue` when metadata lacks it.
- `gclid` is not a GA4 dimension.
- Live budget only on disposable DGTL/test **PAUSED** campaigns in proof notes — fixtures preferred (this PR is fixture-only).

## Spec honesty

`python3 scripts/validate-spec.py` expected:

```
SPEC OK  tools=26 (Consent A kernel)  local_free=35  skills=20
```

Hop maps `--check` clean. `tools/list` freeze count **162** (additive).

## Files (primary)

- `src/ads/recipes-schema.ts`, `src/ads/recipes-gaql.ts`
- `src/budget/mta-ltv.ts`
- `src/tiktok/tiktok-write.ts` (`tiktok_update_campaign_budget`)
- `src/tools/denylist.ts`, `src/google/ga4.ts`
- `src/gateway/hop-catalog.json` + generated hop maps / `w0-2-mutate-parity.json`
- `src/tools/registry.ts`, `src/tools/schemas.ts`
- `schemas/v1/catalog.json`, `tools.schema.json`
- `fixtures/ads/recipes.*.json`, `fixtures/budget/mta-ltv.*.json`
- `skills/mta-ltv-budget/SKILL.md`
- `tests/wave21-budget.test.ts`
- Docs: `docs/TOOLS.md`, `docs/SKILLS.md`

## Tests

Run: `npm test` (fixture / unit only; network guard; no live stamp / Ads / Meta / TikTok budget mutate).

**Local result (2026-09-13):** `557` tests, `0` fail (Wave 20 was `542`; Wave 21 adds `15` in `tests/wave21-budget.test.ts`). `python3 scripts/validate-spec.py` → `SPEC OK  tools=26 (Consent A kernel)  local_free=35  skills=20`. Hop maps `--check` clean.

| Suite | Role |
| --- | --- |
| `tests/wave21-budget.test.ts` | Recipes + fixtures; hop recipe name (no GAQL); plugin single-day `click_view`; gclid refuse; metadata / LTV refuse; DDA-weighted proposal; one-platform sequence; TikTok budget dry_run/confirm; skill laws |
| `tests/mcp-dx-pass2.test.ts` / `wave9-allowlists.test.ts` / `allowlist-parity.test.ts` | Recipe catalog; hop maps; mutate parity |

## Noel atoms (console — leftover)

1. Stamp parity PR: copy hop-catalog (new TikTok budget row + comment) byte-identical; implement the three `gads_search` compilers; Worker builder for `tiktok_update_campaign_budget`.
2. Do **not** mint a mega allocate tool on stamp.
3. Live budget smoke only on disposable DGTL/test **PAUSED** campaigns — fixtures preferred.
4. Orchestrator FF-merge if Origin cannot merge: use this branch tip after the proof commit.
