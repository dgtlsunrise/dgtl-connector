# Wave 16 proof — Meta catalog batch + CAPI (connector half)

Date: 2026-09-13  
Branch: `cursor/wave16-meta-catalog-capi-302c`  
Base: `6762064` (Wave 15 Shopify publications/feeds + productSet)  
Goal: Named closed tools for Marketing API catalog `items_batch` + Conversions API event send. Plugin dual-gates CAPI via stamp. Stamp Worker builders land in a parallel PR on `dgtlsunrise/dgtl-stamp` — hop names / paths here are the byte-identical source.

## What shipped

| Item | Detail |
| --- | --- |
| `meta_catalog_items_batch` | POST `/{GRAPH_API_VERSION}/{catalog_id}/items_batch`. dry_run default. Live confirm `act_{ad_account_id}` AND `catalog_id`. HTTPS `image_link` / `link` only. |
| `meta_get_batch_status` | GET `/{GRAPH_API_VERSION}/{catalog_id}/check_batch_request_status` (`handle` query). Read hop. |
| `meta_create_catalog` | POST `/{GRAPH_API_VERSION}/act_{ad_account_id}/owned_product_catalogs`. Confirm `act_`. Closed `vertical`. |
| `meta_send_capi_events` | POST `/{GRAPH_API_VERSION}/{pixel_id}/events`. Closed `event_name`. SHA-256 `user_data`. Required `event_id`. Confirm `act_` AND `pixel_id`. |
| Polar | Existing Pro **`meta`** bit. No new Polar product. |
| Scopes | `ads_management` and/or `catalog_management` when detectable (`META_SCOPE_MISSING`). |
| Flags | Catalog writes reuse plugin/Worker `META_MUTATE_ENABLED`. CAPI uses **separate** Worker `META_CAPI_ENABLED` (fail-closed). Plugin `DGTL_META_CAPI_ENABLED` defaults on; live hop requires health `meta_capi_enabled===true`. |
| PII | Plugin hashes before hop. Dry-run / hop JSON never contain plaintext email/phone. Audit logs never include args. |
| Secret | App secret / `appsecret_proof` stay on Worker. |

Named tools only. No `meta_mutate`. No Wave 17+ TikTok. Never Axos BM.

## Stamp hop-catalog rows (copy byte-identical)

Stamp PR on `dgtlsunrise/dgtl-stamp` must copy these `tools[]` rows and the new `closed_https_fields` / `gateway_param_allow` keys from this repo’s `src/gateway/hop-catalog.json`, then run `node scripts/generate-hop-maps.mjs`.

```json
{
  "name": "meta_catalog_items_batch",
  "family": "meta",
  "kind": "mutate",
  "method": "POST",
  "path_template": "/{GRAPH_API_VERSION}/{catalog_id}/items_batch"
}
```

```json
{
  "name": "meta_get_batch_status",
  "family": "meta",
  "kind": "read_hop",
  "method": "GET",
  "path_template": "/{GRAPH_API_VERSION}/{catalog_id}/check_batch_request_status"
}
```

```json
{
  "name": "meta_send_capi_events",
  "family": "meta",
  "kind": "mutate",
  "method": "POST",
  "path_template": "/{GRAPH_API_VERSION}/{pixel_id}/events"
}
```

```json
{
  "name": "meta_create_catalog",
  "family": "meta",
  "kind": "mutate",
  "method": "POST",
  "path_template": "/{GRAPH_API_VERSION}/act_{ad_account_id}/owned_product_catalogs"
}
```

### Stamp builder notes (Worker, not this repo)

- `meta_catalog_items_batch`: map `params.items[]` → Graph `requests[]` (`method`, `retailer_id`, `data`). `item_type` default `PRODUCT_ITEM`. `allow_upsert`. Gate on `META_MUTATE_ENABLED`.
- `meta_get_batch_status`: GET path + `handle` query. ads_read.
- `meta_create_catalog`: POST `{name, vertical}` to owned_product_catalogs. Gate on `META_MUTATE_ENABLED`.
- `meta_send_capi_events`: map `params.events[]` → CAPI `data[]` (`event_name`, `event_time`, `event_id`, `action_source`, `event_source_url`, hashed `user_data`, `custom_data.value` from `event_value`). `test_event_code` optional. Attach `appsecret_proof` on Worker. Gate on **`META_CAPI_ENABLED`** (not `META_MUTATE_ENABLED`). Health must expose `meta_capi_enabled`. Never log unhashed PII (plugin already hashes).
- Closed HTTPS fields added: `image_link`, `additional_image_link`, `event_source_url`.

## Locks held

- Consent A kernel stays **26**.
- Polar `meta` only. No TikTok in this PR.
- Plugin dual-gates CAPI via stamp health. Catalog writes keep the existing Meta mutate dual-gate.
- Never unhashed PII in hop / envelopes / audit. Never Axos.
- Named closed tools only.

## Files (primary)

- `src/gateway/hop-catalog.json`, `src/gateway/hop-maps.generated.ts`, `src/gateway/client.ts`
- `src/meta/meta-wave16.ts`
- `src/flags.ts`, `src/errors.ts`, `src/support/matrix.ts`, `src/support/runbooks.ts`, `src/auth/doctor.ts`
- `src/tools/schemas.ts`, `src/tools/registry.ts`
- `fixtures/meta/catalog.items_batch.json`, `catalog.batch_status.json`, `catalog.create.json`, `capi.events.json`
- `tests/meta-wave16.test.ts`
- Docs: `docs/TOOLS.md`, `docs/ERRORS.md`, `docs/PERMISSIONS.md`, `docs/ops/RUNBOOKS.md`

## Tests

Run: `npm test` (fixture / unit only; network guard; no live Graph / CAPI).

**Local result (2026-09-13):** `487` tests, `0` fail. `python3 scripts/validate-spec.py` → `SPEC OK tools=26 skills=17`. Hop maps `--check` clean.

| Suite | Role |
| --- | --- |
| `tests/meta-wave16.test.ts` | Flag / Polar / dry_run / confirm / HTTPS images / hashed PII / Worker CAPI fail-closed / hop names / fixtures |
| `tests/allowlist-parity.test.ts` | hop-catalog mutate/read classification |
| `tests/wave9-allowlists.test.ts` | tools/list freeze + hop maps `--check` + `META_CAPI_NOT_ENABLED` runbook |

## Noel atoms (console — leftover)

1. Stamp PR must copy hop-catalog rows **byte-identical** and add Worker builders + `META_CAPI_ENABLED` (fail-closed) + health `meta_capi_enabled`.
2. App Review: `ads_management` and/or `catalog_management`. Do not put the app secret in the plugin.
3. Live smoke only on a disposable DGTL ad account / catalog / pixel. Fixtures preferred. Never Axos BM.
4. Wave 17+ TikTok stays out of this PR.
