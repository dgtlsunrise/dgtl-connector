# Wave 14 proof — Merchant API ProductInput writes (Consent MC)

Date: 2026-09-13  
Branch: `cursor/wave14-mc-product-input-3dac`  
Base: `7965025` (Wave 13 sGTM clients + local 444-pass proof)  
Goal: Upsert products into an API data source so Shopping ads have inventory. Writes are Merchant API **ProductInput** (not processed Product).

## What shipped

| Item | Detail |
| --- | --- |
| HTTP | `GoogleMcWriteHttp` — merchantapi.googleapis.com only; closed POST/PATCH/DELETE allowlist |
| Reads stay GET-only | `GoogleHttp` + `GET_ONLY_HOSTS` still refuse Merchant mutate |
| Writes | Same Consent MC AuthPort (`content`). Never Consent A. Polar JWT `ads` (no `mc` bit) |
| `mc_create_data_source` | API-type primary/supplemental (`ONLINE_PRODUCTS`). No `fileInput` |
| `mc_upsert_product_input` | `productInputs.insert` (default) or `patch` when `update_mask` is set. Requires `data_source` |
| `mc_delete_product_input` | `productInputs.delete` + `dataSource` query |
| `mc_fetch_data_source` | FILE source `:fetch` (operator). Not Product insert |
| Confirm | Live `confirm` / `confirm_phrase` must include digits `merchant_id` |
| Flag | Live hops need `DGTL_WRITES_ENABLED`. `dry_run` default true (zero HTTP after Polar) |

Named tools only. No data-source delete/patch, promotions, reviews, or processed Product POST. Never Axos. No Wave 15+.

## Locks held

- Polar stays on JWT `ads`. Consent MC is already write-capable; no Consent A scope added.
- Never guess `merchant_id`. Missing id → `RESOURCE_REQUIRED` (zero hop).
- Insert without `data_source` → `RESOURCE_REQUIRED` (zero hop).
- Write `ProductInput` with `dataSource` query, not `/products` processed Product.
- Kernel stays **26**. Write tools are gated `LICENSE_REQUIRED` (not Consent A).

## Files (primary)

- `src/http/google-mc-write.ts`
- `src/google/mc-write.ts`, `src/google/mc.ts`, `src/ids.ts`
- `src/context.ts`, `src/http/google.ts`
- `src/tools/schemas.ts`, `src/tools/registry.ts`
- `fixtures/google/mc/dataSources.create.json`, `dataSources.fetch.json`, `productInputs.insert.json`, `productInputs.patch.json`, `productInputs.delete.json`
- `tests/mc-write.test.ts`
- Docs: `docs/TOOLS.md`, `docs/PERMISSIONS.md`, `docs/ERRORS.md`

## Tests

Run: `npm test` (fixture / unit only; network guard; no live googleapis mutate).

**Local result (2026-09-13):** `462` tests, `0` fail. `python3 scripts/validate-spec.py` → `SPEC OK tools=26 skills=17`.

| Suite | Role |
| --- | --- |
| `tests/mc-write.test.ts` | Polar / Consent A lock; dry_run; missing merchant_id; missing data_source; confirm; insert fixture + dataSource query; patch/delete/fetch; GET-only read client |
| `tests/mc-content-api.test.ts` | Reads unchanged; `mc_insert_product` / `mc_mutate` stay unregistered |
| `scripts/validate-spec.py` | Kernel 26; gated write tools present |

## Noel atoms (console — leftover)

1. Do **not** add `content` to the free Consent A Desktop client.
2. Live smoke only on a **disposable DGTL** Merchant Center. Fixtures preferred.
3. ProductInput processing can lag several minutes before `mc_list_products` shows the SKU.
