# Wave 10 proof — Consent G / Consent S plumbing

Date: 2026-09-13  
Branch: `cursor/wave-10-consent-gs-8e8f`  
Goal: Make GA4 Admin and GSC writes *possible to add later* without touching Consent A. Ship Consent G / Consent S scopes, login CLIs, token stores, doctor/packet booleans, error codes, and intersection CI. **No live GA4/GSC mutate HTTP.** No Admin write tools in `tools/list` (optional `google_whoami` connection flags only).

## What shipped

| Item | Detail |
| --- | --- |
| `CONSENT_G` | `https://www.googleapis.com/auth/analytics.edit` only (never blanket `analytics`) |
| `CONSENT_S` | `https://www.googleapis.com/auth/webmasters` (write) |
| Login | `auth login-ga4-admin` → `PLUGIN_DATA/google-oauth-ga4-admin.json` (0600) |
| Login | `auth login-gsc-write` → `PLUGIN_DATA/google-oauth-gsc-write.json` (0600) |
| Env locals | `.env.ga4-admin.local` / `.env.gsc-write.local` (Consent W `.env.write.local` style) |
| Errors | `CONSENT_G_REQUIRED`, `CONSENT_S_REQUIRED` (+ `docs/ERRORS.md`, error schema, runbooks) |
| Doctor / packet | Store **existence** booleans only — never tokens (`stores.consent_g` / `stores.consent_s`) |
| whoami | `consent_g` / `consent_s` `{ present, host_injected }` flags |
| Writes flag | Login CLIs do **not** flip `DGTL_WRITES_ENABLED` |
| Consent A | Still readonly forever; write tools still absent from `CONSENT_A_TOOLS` |

## Files (primary)

- `src/google/scopes.ts`
- `src/auth/types.ts`, `src/auth/store.ts`, `src/auth/port.ts`, `src/auth/host-injected.ts`
- `src/auth/write-env-local.ts`, `src/auth/login-cli.ts`, `src/auth/doctor.ts`, `src/auth/pkce.ts`
- `src/index.ts`, `src/context.ts`, `src/errors.ts`, `src/google/whoami.ts`
- `src/support/matrix.ts`, `src/support/runbooks.ts`, `src/gateway/client.ts`
- `schemas/v1/error.schema.json`
- `tests/consent-gs.test.ts`
- Docs: `docs/ERRORS.md`, `docs/PERMISSIONS.md`, `docs/TOOLS.md`, `docs/ARCHITECTURE.md`, `docs/ops/PRODUCT-DESIGN.md`, `docs/ops/NOEL-ONLY-CHECKLIST.md`, `docs/ops/RUNBOOKS.md`, `skills/license-and-reconnect/SKILL.md`, `.env.example`

## Tests

Run: `npm test` (fixture / unit only; network guard; no googleapis mutate).

**Local result (2026-09-13):** `390` tests, `0` fail. `python3 scripts/validate-spec.py` → `SPEC OK tools=24 skills=17`.

| Suite | Count | Role |
| --- | --- | --- |
| `tests/consent-gs.test.ts` | 14 | A ∩ G = ∅, A ∩ S = ∅, A ∩ W ∩ C still hold; login scopes; 0600 stores; AuthPort isolation; env-local no A bleed; doctor/packet/whoami booleans; `CONSENT_A_TOOLS` has no write tools; flag stays false |
| Full `npm test` | 390 pass | Includes existing consent-w / consent-c / Waves 4–9 invariants |

## Noel atoms (console — RED leftover)

Agents cannot create Google OAuth clients. Until live Admin/GSC writes are needed:

1. **Do not** add `analytics.edit` or `webmasters` (write) to the free Consent A Desktop client.
2. Optional: create **separate** Desktop clients for Consent G and Consent S (or deliberately extend Consent W — never A). See `docs/ops/NOEL-ONLY-CHECKLIST.md` G1–G5.
3. Put client id/secret in gitignored `.env.ga4-admin.local` / `.env.gsc-write.local` only.

No Axos customer `2859537899`. No Wave 11+ mutate tools in this PR.

## Locks held

- Consent A / free Desktop client stays readonly.
- Named closed tools only; no mega-mutate.
- No live GA4 Admin or GSC write HTTP.
