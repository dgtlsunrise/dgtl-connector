# Wave 18 proof — Klaviyo local `pk_` lane

Date: 2026-09-13  
Branch: `cursor/wave18-klaviyo-pk-lane-820c`  
Base: `3eb19f5` (Wave 17 TikTok catalog + Events API)  
Goal: Named closed tools for Klaviyo account / sparse profiles / lists / flows / campaigns / metrics, plus draft campaign create, profile upsert, and backfill event. **Local `pk_` only.** No send job (Wave 19/22). Catalog + reviews deferred to Wave 19.

## What shipped

| Item | Detail |
| --- | --- |
| `klaviyo_get_account` | GET `/api/accounts`. Whoami. Copy account id for write confirms. |
| `klaviyo_list_profiles` / `klaviyo_get_profile` | Sparse `email,created,updated,external_id`. Optional `first_name` / `last_name` only. Strips phone / location / properties. |
| `klaviyo_list_lists` / `klaviyo_list_segments` | Paginated. No CSV import mega-tool. |
| `klaviyo_list_flows` / `klaviyo_get_flow` | Paginated list then get by `flow_id`. |
| `klaviyo_list_campaigns` | Channel filter required (default `email`). Closed `email` / `sms` / `mobile_push`. |
| `klaviyo_list_metrics` | Metrics catalog. Not Metric Aggregates passthrough. |
| `klaviyo_create_campaign` | **Draft only** `POST /api/campaigns`. Never `POST /api/campaign-send-jobs`. |
| `klaviyo_upsert_profile` | `POST /api/profile-import`. Closed fields. No properties bag. |
| `klaviyo_create_event` | `POST /api/events`. `backfill` defaults **true**. Closed flat properties. |
| Auth | `KLAVIYO_API_KEY` / `DGTL_KLAVIYO_API_KEY` / `KLAVIYO_PRIVATE_KEY` or `PLUGIN_DATA/klaviyo.json`. Shape `pk_…`. Fail `KLAVIYO_NOT_CONNECTED`. Never log the key. |
| Revision | Official header **`2026-07-15`**. Host `a.klaviyo.com`. |
| Writes | `DGTL_WRITES_ENABLED` (default off) + `confirm_phrase` containing the account id. `dry_run` default true. Fail order: `WRITE_NOT_ENABLED` → `KLAVIYO_NOT_CONNECTED` → dry-run GET account (zero mutate POST) → live confirm. |

Named tools only. **LOCAL_FREE** — not Polar, not stamp, not Consent A. No Wave 19 catalog/reviews. Never Axos.

## Locks held

- Consent A kernel stays **26**.
- Klaviyo is **not** in the Polar / Consent A kernel. No Polar `klaviyo` OAuth. No stamp hop (`stampHop` unset).
- No campaign send job. No catalog items. No reviews.
- Writes are local flag + account-id confirm. Marketplace default stays `DGTL_WRITES_ENABLED=false`.
- Doctor / `auth status` report **existence / presence only**. Never print the `pk_`.
- Never Axos. Named closed tools only. Fixtures preferred over live.

## Spec honesty (Consent A vs local-free)

`python3 scripts/validate-spec.py`:

```
SPEC OK  tools=26 (Consent A kernel)  local_free=29  skills=18
```

| Bucket | Count | What |
| --- | --- | --- |
| Consent A kernel (`catalog.json` `tools[]`) | **26** | Free Google GA4/GSC/GTM. Unchanged. |
| Local-free gated | **29** | Shopify reads (10) + Shopify writes (2) + GBP reads (5) + Klaviyo reads (9) + Klaviyo writes (3). |
| Skills | **18** | Prior 17 + `klaviyo-readonly`. |

Klaviyo reads fail `KLAVIYO_NOT_CONNECTED`. Klaviyo writes fail `WRITE_NOT_ENABLED` (then `KLAVIYO_NOT_CONNECTED` at runtime). Neither is `LICENSE_REQUIRED`.

Hop maps `--check` clean. Klaviyo is **not** in `src/gateway/hop-catalog.json`.

## Files (primary)

- `src/klaviyo/auth.ts`, `http.ts`, `klaviyo.ts`, `klaviyo-write.ts`
- `src/tools/registry.ts`, `src/tools/schemas.ts`
- `src/errors.ts`, `src/auth/doctor.ts`, `src/auth/login-cli.ts`, `src/index.ts`
- `src/support/matrix.ts`, `src/support/runbooks.ts`
- `schemas/v1/catalog.json`, `tools.schema.json`, `error.schema.json`
- `scripts/validate-spec.py`
- `fixtures/klaviyo/*.json`
- `tests/klaviyo-readonly.test.ts`, `tests/klaviyo-write.test.ts`
- Skill: `skills/klaviyo-readonly/SKILL.md`
- Docs: `docs/TOOLS.md`, `docs/ERRORS.md`, `docs/PERMISSIONS.md`, `docs/SKILLS.md`, `docs/ops/RUNBOOKS.md`

## Tests

Run: `npm test` (fixture / unit only; network guard; no live Klaviyo).

**Local result (2026-09-13):** `520` tests, `0` fail. `python3 scripts/validate-spec.py` → `SPEC OK  tools=26 (Consent A kernel)  local_free=29  skills=18`. Hop maps `--check` clean. Secret heuristics clean (no live `pk_`).

| Suite | Role |
| --- | --- |
| `tests/klaviyo-readonly.test.ts` | LOCAL_FREE / not Consent A / not Polar / no stampHop; sparse PII strip; revision header; `KLAVIYO_NOT_CONNECTED` zero HTTP; file-store resolve; send-job/catalog/reviews path refuse |
| `tests/klaviyo-write.test.ts` | `WRITE_NOT_ENABLED` first; dry-run GET account only; live confirm must contain account id `W18aCc`; draft POST never hits send jobs; event `backfill` defaults true |
| `tests/packaging.test.ts` / `proof.test.ts` / `wave9-allowlists.test.ts` | Kernel stays 26; Klaviyo gated rows; runbooks for `KLAVIYO_*` |
| `tests/doctor.test.ts` | `klaviyo.json` existence-only; never prints `pk_secret_must_not_print_xx` |

## Noel atoms (console — leftover)

1. Live smoke only on a disposable DGTL Klaviyo account. Fixtures preferred. Never log the key. Never Axos.
2. Wave 19 may add catalog + reviews and (later) a campaign send job. Do **not** fold Klaviyo into Consent A or Polar in that wave unless product explicitly opens a Polar `klaviyo` product.
3. Polar `klaviyo` OAuth stays later. This PR is the local `pk_` lane only.
4. Orchestrator FF-merge if Origin cannot merge: use this branch tip after the proof commit.
