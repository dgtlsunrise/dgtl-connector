# Wave 11 proof — GA4 Admin writes (Consent G) + Ads-id recipes

Date: 2026-09-13  
Branch: `cursor/wave11-ga4-admin-39ce`  
Base: `d10bc34` (Wave 10 Consent G/S plumbing)  
Goal: Chat can stand up a GA4 web stream, key events, Ads link, attribution settings, and closed Ads-id / conversion report recipes. Writes use Consent G (`analytics.edit`). Reads stay Consent A where Admin GET allows it.

## What shipped

| Item | Detail |
| --- | --- |
| HTTP | `GoogleGa4AdminHttp` — analyticsadmin allowlist, GET/POST/PATCH/DELETE, Consent G token only |
| Consent A lock | `GoogleHttp` refuses non-GET on `analyticsadmin.googleapis.com` |
| Reads on A | `ga4_list_google_ads_links` (v1beta GET, documented readonly); `ga4_get_attribution_settings` (v1alpha GET, documented readonly) |
| Writes on G | Ads link create/delete; attribution PATCH (v1alpha); web stream create/update; key event create/update; custom dimension/metric create; MP secret create; property create |
| G read | `ga4_list_mp_secrets` — Consent G; `secretValue` redacted in envelope and never in call logs |
| Confirm | Live mutate `confirm_phrase` must include `properties/{id}` (or `accounts/{id}` on create property) |
| Flag | `DGTL_WRITES_ENABLED` still gates writes; login does not flip it |
| Reports | `ga4_run_report` Ads-id recipes; `searchQuery` denylist kept; no `ga4_run_conversion_report` |
| Spike | `docs/ops/GA4-CONVERSIONSPEC-SPIKE.md` — v1beta has no `conversionSpec` |

Named tools only. No mega-mutate. No Wave 12 GSC writes. No `ga4_update_property`.

## Locks held

- Consent A never receives write scopes. G store = `google-oauth-ga4-admin.json`.
- Write tools **not** in `CONSENT_A_TOOLS` (family `ga4_write`). Kernel stays **24**.
- `A ∩ G = ∅` still (`tests/consent-gs.test.ts`, `tests/ga4-admin-write.test.ts`).
- Locked numeric id `2859537899` refused on normalize (zero HTTP).
- MP `secretValue` never in `HttpCall` JSON; list envelopes redact it.

## Files (primary)

- `src/http/google-ga4-admin.ts`, `src/http/google.ts`
- `src/context.ts`, `src/ids.ts`
- `src/google/ga4-write.ts`, `src/google/ga4-ads-id.ts`, `src/google/ga4.ts`
- `src/tools/schemas.ts`, `src/tools/registry.ts`
- `fixtures/google/ga4/*` Wave 11 Admin shapes
- `tests/ga4-admin-write.test.ts`, `tests/ga4-ads-id-recipe.test.ts`
- Docs: `docs/TOOLS.md`, `docs/PERMISSIONS.md`, `docs/ERRORS.md`, `docs/ops/GA4-CONVERSIONSPEC-SPIKE.md`, `skills/ga4-report-recipes/SKILL.md`

## Tests

Run: `npm test` (fixture / unit only; network guard; no live googleapis mutate).

| Suite | Role |
| --- | --- |
| `tests/ga4-admin-write.test.ts` | Flag / Consent G / dry_run / confirm / path allowlist / A reads / G writes / MP redaction / locked id |
| `tests/ga4-ads-id-recipe.test.ts` | Denylist + Ads-id recipes + keyword-id refuse + `key_event_names` |
| `tests/consent-gs.test.ts` | `A ∩ G = ∅`; write tools absent from `CONSENT_A_TOOLS` |

## Noel atoms (console — leftover)

1. Do **not** add `analytics.edit` to the free Consent A Desktop client.
2. Live smoke only on a **disposable DGTL** property. Fixtures preferred. Never customer `2859537899`.
3. Attribution GET/PATCH is **v1alpha** until Google promotes it to v1beta.
