# Wave 13 proof — sGTM clients, server container create, environments

Date: 2026-09-13  
Branch: `cursor/wave13-sgtm-clients-d98b`  
Base: `9a7b4df` (Wave 12 GSC sitemap submit/delete + local test proof)  
Goal: List and create/update sGTM **clients** in chat; optional server container create; environments list/create. Publish remains existing Consent W `gtm_publish_container`.

## What shipped

| Item | Detail |
| --- | --- |
| List on A | `gtm_list_clients`, `gtm_list_environments` — Consent A `tagmanager.readonly` (official list allows readonly **or** edit; we use A) |
| Writes on W | `gtm_create_client`, `gtm_update_client`, `gtm_create_container`, `gtm_create_environment` — `tagmanager.edit.containers` + `DGTL_WRITES_ENABLED` |
| Client `type` | Closed enum: `gaawp`, `googtag`, `gclidw`, `flc`, `ua`, `mp`. Unknown → `INVALID_ARGUMENT`, zero HTTP |
| `usageContext` | Closed enum: `server` (sGTM) + locked `web` / `android` / `ios` / `amp` |
| Confirm | Client/env live mutate: `publicId` **or** `accounts/{id}/containers/{id}`. Create container: `accounts/{id}` |
| Flag | `DGTL_WRITES_ENABLED` false → `WRITE_NOT_ENABLED`, zero HTTP |
| Spike | `docs/ops/GTM-CLIENTS-SPIKE.md` |

Named tools only. Publish unchanged. No stamp conversion sinks (Wave 20). Ingest-key parameter keys refused. Never Axos. No Wave 14+ (folders, users, reauthorize, GSC add-site).

## Locks held

- Consent A never receives write scopes. Writes use Consent W + `DGTL_WRITES_ENABLED`.
- Write tools **not** in `CONSENT_A_TOOLS` (family `gtm_write`).
- Kernel count is **honest 26** (was 24; added the two Consent A list tools). `catalog.json` / `plugin.json` / `CONSENT_A_KERNEL_COUNT` / `docs/TOOLS.md` agree.
- `A ∩ W = ∅` still.

## Files (primary)

- `src/google/gtm-types.ts`, `src/google/gtm.ts`, `src/google/gtm-write.ts`
- `src/http/google-write.ts`
- `src/tools/schemas.ts`, `src/tools/registry.ts`
- `fixtures/google/gtm/clients.*.json`, `environments.*.json`, `containers.create.json`
- `tests/gtm-clients.test.ts`
- Docs: `docs/TOOLS.md`, `docs/PERMISSIONS.md`, `docs/ERRORS.md`, `docs/ops/GTM-CLIENTS-SPIKE.md`

## Tests

**Local result (2026-09-13):** `444` tests, `0` fail. `python3 scripts/validate-spec.py` → `SPEC OK tools=26 skills=17`. Fixture / unit only; network guard; no live googleapis mutate.

| Suite | Role |
| --- | --- |
| `tests/gtm-clients.test.ts` | List on A; flag off zero HTTP; unknown type; ingest-key refuse; dry_run; confirm publicId/path; create container/env; allowlist |
| `tests/consent-w.test.ts` | New writes gated; not in kernel |
| `tests/contract.test.ts` | Kernel 26 fixture loop includes list clients/envs |
| `scripts/validate-spec.py` | catalog count 26; gated write tools `WRITE_NOT_ENABLED` |

## Noel atoms (console — leftover)

1. Do **not** add `tagmanager.edit.containers` / `tagmanager.publish` to the free Consent A Desktop client.
2. Live smoke only on a **disposable DGTL** server container. Fixtures preferred.
3. Stamp ingest / CAPI remains Wave 20 (wrong repo for Worker code).
