# Wave 20 proof — conversion fabric (connector half)

Date: 2026-09-13  
Branch: `cursor/wave20-conversion-fabric-4ba1`  
Base: `e755261` (Wave 19 catalog fan-out + Klaviyo catalog/reviews)  
Goal: Thin plugin surface so operators can see sink health and trigger an apply-path ingest test. Real funded uploads live on stamp (`FundedUploadSink`). Replaces the product story of `NoNetworkUploadSink`.

## What shipped

| Item | Detail |
| --- | --- |
| `conversion_fabric_status` | Always callable. Sink / flag health + hop contracts. Never keys, JWT, or `user_data`. |
| `sgtm_ingest_test` | HTTP `POST {gateway}/v1/sgtm/ingest`. Apply header only. Closed `event_name=apply`. Plugin flag **default off**. |
| Reuse Wave 16–17 | `meta_send_capi_events`, `tiktok_track_events`. No browser CAPI/Events with secrets. |
| Ads Data Manager | Stamp-internal `IngestEvents` only. **No** plugin send tool. **Not** `UploadClickConversions`. |
| Polar `sgtm` | Reserved, default-off, **not minted**. `hasFeature` stays `ads` \| `meta` \| `tiktok`. |
| Skill | Strengthened `gtm-readonly-limits` — forbid funded **and** apply keys in **web** GTM. Still **19** skills. |

Named closed tools only. Consent A kernel stays **26**. Never Axos. No Wave 21 budget skill.

## Stamp hop / sink names (parity contract)

Copy `src/gateway/hop-catalog.json` `conversion_fabric` byte-identical into stamp, then `node scripts/generate-hop-maps.mjs`. Stamp implements `FundedUploadSink`.

| id | `stamp_sink` | `stamp_hop` | Plugin send tool | Worker health key |
| --- | --- | --- | --- | --- |
| `ads_data_manager` | `AdsDataManagerIngestEventsSink` | `ads_data_manager_ingest_events` | `null` (stamp-internal) | `ads_data_manager_enabled` |
| `meta_capi` | `MetaCapiEventsSink` | `meta_send_capi_events` | `meta_send_capi_events` | `meta_capi_enabled` |
| `tiktok_events` | `TikTokEventsSink` | `tiktok_track_events` | `tiktok_track_events` | `tiktok_events_enabled` |

Ads Data Manager hop (stamp, not this plugin):

- `POST https://datamanager.googleapis.com/v1/events:ingest`
- RPC `IngestEvents`
- Scope `https://www.googleapis.com/auth/datamanager`
- **Not** deprecated `UploadClickConversions`

sGTM ingest (HTTP, not a hop-catalog MCP hop):

- Path `/v1/sgtm/ingest`
- Apply header `X-DGTL-Apply-Key` (plugin host env `DGTL_SGTM_APPLY_KEY` / `DGTL_APPLY_KEY`)
- Funded header `X-DGTL-Ingest-Key` — **never** sent from this plugin; **never** in web GTM
- Worker flag `SGTM_INGEST_ENABLED` / health `sgtm_ingest_enabled` (fail-closed)
- Plugin test tool `sgtm_ingest_test`; closed event `apply` only

Polar: `{ "feature": "sgtm", "default": "off", "mint": false }`

## Locks held

- Consent A kernel stays **26**.
- Polar `sgtm` documented only — do not mint.
- Funded key never in web GTM (skill + GTM client param refuse + ingest test refuse).
- Prefer Data Manager `IngestEvents` for Ads conversion upload.
- Never log `user_data` plaintext.
- Never Axos.
- No Wave 21 budget skill.
- Named closed tools only.

## Spec honesty

`python3 scripts/validate-spec.py` expected:

```
SPEC OK  tools=26 (Consent A kernel)  local_free=35  skills=19
```

Hop maps `--check` clean. `tools/list` freeze count **161** (additive).

## Files (primary)

- `src/conversion/fabric.ts`
- `src/gateway/hop-catalog.json` (`conversion_fabric`)
- `src/flags.ts`, `src/support/matrix.ts`, `src/auth/doctor.ts`, `src/errors.ts`
- `src/tools/registry.ts`, `src/tools/schemas.ts`
- `schemas/v1/catalog.json`, `tools.schema.json`, `error.schema.json`
- `fixtures/conversion/fabric.status.json`, `sgtm.ingest.apply.json`
- `tests/wave20-conversion-fabric.test.ts`
- Skill: `skills/gtm-readonly-limits/SKILL.md`
- Docs: `docs/TOOLS.md`, `docs/ERRORS.md`, `docs/SKILLS.md`, `docs/PERMISSIONS.md`, `docs/ops/RUNBOOKS.md`

## Tests

Run: `npm test` (fixture / unit only; network guard; no live stamp / Ads / Meta / TikTok / sGTM).

**Local result (2026-09-13):** `542` tests, `0` fail. `python3 scripts/validate-spec.py` → `SPEC OK  tools=26 (Consent A kernel)  local_free=35  skills=19`. Hop maps `--check` clean.

## Noel atoms (console — leftover)

1. Stamp half implements `FundedUploadSink` + health keys `ads_data_manager_enabled` / `sgtm_ingest_enabled`. Copy `conversion_fabric` byte-identical.
2. Do **not** mint Polar `sgtm`.
3. Do **not** put funded or apply keys in web GTM.
4. Ads new adopters: Data Manager `IngestEvents`, not `UploadClickConversions`.
5. Orchestrator FF-merge if Origin cannot merge: use this branch tip after the proof commit.
