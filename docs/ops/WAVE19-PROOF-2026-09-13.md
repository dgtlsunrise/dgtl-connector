# Wave 19 proof — catalog fan-out + Klaviyo catalog/reviews

Date: 2026-09-13  
Branch: `cursor/wave19-catalog-fan-out-klaviyo-ea22`  
Base: `7f79227` (Wave 18 Klaviyo local `pk_` lane)  
Goal: Skill `catalog-fan-out` maps Shopify → MC / Meta / TikTok / Klaviyo with **per-network validation** (no mega upsert-all tool). Klaviyo catalog + reviews on the local `pk_` lane. Three-product fixture proves Google omits invalid rows. Isolation: refuse unnamed `merchant_id`.

## What shipped

| Item | Detail |
| --- | --- |
| Skill `catalog-fan-out` | Judgment only. Four destinations, four named write tools. Mapping does not write. |
| Google / MC | Named `merchant_id` required. Omit missing GTIN (unless brand **and** MPN) or missing / non-HTTPS image or link. Write: `mc_upsert_product_input`. |
| Meta | Omit missing HTTPS image or landing URL. GTIN not required. Write: `meta_catalog_items_batch`. |
| TikTok | Same image/URL omit rule. `sku_id` is Events `content_id`. Write: `tiktok_upload_catalog_products`. |
| Klaviyo catalog map | sku + title + HTTPS url; image optional; `external_id` = sku; `$custom` / `$default` only. Write: `klaviyo_upsert_catalog_items`. |
| `klaviyo_list_catalog_items` / `_categories` / `_variants` | GET allowlisted collection paths. Sparse fields. |
| `klaviyo_upsert_catalog_items` | Closed `POST /api/catalog-item-bulk-create-jobs` and/or `…-update-jobs`. Cap 20. `$custom` compound ids only. |
| `klaviyo_list_reviews` / `klaviyo_get_review` | Sparse — `email` / `author` stripped. |
| Three-product fixture | Tote (no GTIN), beanie (no image), mug (valid). Google keeps the mug only. |
| Isolation | Unnamed `merchant_id` (`undefined`, `""`, `"default"`, `"first"`, `"0"`) → `RESOURCE_REQUIRED`. |

Named tools + skill only. **LOCAL_FREE** Klaviyo — not Polar, not stamp, not Consent A. Never Axos. Live fan-out still requires **each destination’s write grant**.

## Locks held

- Consent A kernel stays **26**.
- No Polar `klaviyo` OAuth (Wave **19b** deferred). No stamp hop (`stampHop` unset).
- No mega upsert-all / `fan_out_live` tool. Skill says live still needs each destination’s grant.
- Do **not** invent `$shopify:::$default:::` ids. Custom upserts use `external_id` (sku). Copy `$custom:::$default:::` ids from list tools.
- No campaign send jobs (Wave 22). No Wave 20 conversion fabric.
- Writes: `DGTL_WRITES_ENABLED` (default off) + `confirm_phrase` containing the Klaviyo account id. Fail order: `WRITE_NOT_ENABLED` → `KLAVIYO_NOT_CONNECTED` → dry-run GET account (zero catalog POST) → live confirm.
- Never Axos. Fixtures preferred over live.

## Spec honesty (Consent A vs local-free)

`python3 scripts/validate-spec.py`:

```
SPEC OK  tools=26 (Consent A kernel)  local_free=35  skills=19
```

| Bucket | Count | What |
| --- | --- | --- |
| Consent A kernel (`catalog.json` `tools[]`) | **26** | Free Google GA4/GSC/GTM. Unchanged. |
| Local-free gated | **35** | Prior 29 + Klaviyo catalog/reviews reads (5) + `klaviyo_upsert_catalog_items` (1). |
| Skills | **19** | Prior 18 + `catalog-fan-out`. |

Klaviyo catalog/reviews reads fail `KLAVIYO_NOT_CONNECTED`. Catalog upsert fails `WRITE_NOT_ENABLED` first. Neither is `LICENSE_REQUIRED`.

Hop maps `--check` clean. Klaviyo is **not** in `src/gateway/hop-catalog.json`.

## Files (primary)

- `src/catalog/fan-out.ts`
- `src/klaviyo/http.ts`, `klaviyo.ts`, `klaviyo-write.ts`
- `src/tools/registry.ts`, `src/tools/schemas.ts`
- `schemas/v1/catalog.json`, `tools.schema.json`
- `scripts/validate-spec.py`
- `fixtures/catalog/fan-out.three-products.json`
- `fixtures/klaviyo/catalog.*.json`, `reviews.list.json`, `review.get.json`
- `tests/catalog-fan-out.test.ts`, `tests/klaviyo-catalog.test.ts`
- Skill: `skills/catalog-fan-out/SKILL.md` (plus `skills/klaviyo-readonly` catalog/reviews notes)
- Docs: `docs/TOOLS.md`, `docs/SKILLS.md`, `docs/PERMISSIONS.md`, `docs/ops/RUNBOOKS.md`

## Tests

Run: `npm test` (fixture / unit only; network guard; no live Klaviyo / MC / Meta / TikTok writes).

**Local result (2026-09-13):** `532` tests, `0` fail. `python3 scripts/validate-spec.py` → `SPEC OK  tools=26 (Consent A kernel)  local_free=35  skills=19`. Hop maps `--check` clean. Secret heuristics clean (no live `pk_`).

| Suite | Role |
| --- | --- |
| `tests/catalog-fan-out.test.ts` | Skill exists; no mega upsert-all tool; three-product Google omit; unnamed `merchant_id` refused; `$shopify:::` refused |
| `tests/klaviyo-catalog.test.ts` | Catalog/reviews fixtures; review email/author strip; `WRITE_NOT_ENABLED` then dry-run GET account only; live bulk-create `$custom`; builders refuse `$shopify:::` |
| `tests/klaviyo-readonly.test.ts` | LOCAL_FREE / not Consent A / no stampHop; catalog/reviews path allowlist; closed schemas; `sparsifyReview` |
| `tests/klaviyo-write.test.ts` | Existing draft/upsert/event fail order unchanged |
| `tests/packaging.test.ts` / `proof.test.ts` / `wave9-allowlists.test.ts` | Kernel stays 26; gated Klaviyo rows; tools/list freeze count 159 |

## Noel atoms (console — leftover)

1. Live fan-out only after each destination’s grant (MC Polar `ads` + Consent MC; Meta/TikTok Polar + stamp + mutate flags; Klaviyo local `pk_` + writes flag). Mapping is not a write.
2. Polar `klaviyo` OAuth is Wave **19b**, not this PR. Do not add a stamp hop for Klaviyo.
3. Do not invent `$shopify:::$default:::` ids or GTINs to “fix” Google omissions.
4. Wave 20 conversion fabric and campaign send jobs stay out.
5. Orchestrator FF-merge if Origin cannot merge: use this branch tip after the proof commit.
