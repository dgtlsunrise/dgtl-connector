{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "dgtl-connector": {
      "type": "stdio",
      "command": "./bin/dgtl-connector-mcp",
      "cwd": "${PLUGIN_ROOT}",
      "env": {
        "PLUGIN_DATA": "${PLUGIN_DATA}"
      }
    }
  }
}
